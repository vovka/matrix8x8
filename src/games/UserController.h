#ifndef UserController_h
#define UserController_h

class UserController
{
public:
  // UserController();
  // ~UserController();
  void readInput(bool, bool, bool, bool);
  unsigned char state = 0;
private:
  int i = 0;
};

#endif
